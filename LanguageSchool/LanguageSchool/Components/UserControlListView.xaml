<UserControl x:Class="LanguageSchool.Components.UserControlListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LanguageSchool.Components"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="175"
             Background="White">
    <Grid>
        <Border BorderBrush="Black" BorderThickness="3" Margin="3">
            <StackPanel>

            <Image Height="50" Width="50" Margin="10" Source="{Binding PhotoByte}" Name="ServiceImg">
            </Image>
           
                <Button Width="110" Name="MainBnt" Click="MainBnt_Click" >
                    Сделать главным
                </Button>
            <Button Width="60" Name="DeletePhotoBtn" Click="DeletePhotoBtn_Click">
                Удалить
            </Button>
           
            
        </StackPanel>
            </Border>

    </Grid>
</UserControl>
